# 一期 AI 产品化方案

## Summary
目标是把当前产品做成“面向产品经理的 AI 产品工作台”，先让用户从自然语言需求直接产出结构化交付物，再逐步过渡到设计与代码。第一期不绑定 Claude/Codex 客户端本体，而是采用 `统一 Agent 协议 + 可切换模型提供方`：你定义技能、状态机、输出格式和确认节点，Claude/Codex 只是底层模型。

一期的标准工作流定为：
1. 创建项目
2. 用户输入项目目标/需求
3. AI 批处理生成：`需求规格说明书 + 功能清单 + 页面结构 + 页面草图`
4. 用户在每个阶段可编辑、确认、回退
5. 用户选择草图和样式后，AI 生成 `HTML 原型页面`
6. 导出 HTML；Figma/Axure 放到后续适配层，不作为一期主交付

默认交互模式采用你选的 `半自动批处理`：
- 用户一次选择“生成到哪个阶段”
- AI 连续执行多个技能
- 到阶段边界自动停下，等待确认

## Key Changes
### 1. 把 AI 从“聊天框”升级为“流程编排器”
在现有 AI 模块上新增一个工作流编排层，职责是：
- 根据当前项目状态决定下一步该调用哪个技能
- 为每个技能准备上下文
- 校验输出是否符合结构
- 把结果写回你现有的数据模型
- 在阶段边界暂停，等待用户确认

一期工作流状态机固定为：
- `project_brief`
- `requirements_spec`
- `feature_tree`
- `page_structure`
- `wireframes`
- `html_prototype`

阶段边界固定为 3 个批处理包：
- `需求包`：需求规格说明书 + 功能清单
- `原型包`：页面结构 + 页面草图
- `页面包`：HTML 原型

### 2. 技能设计不要做成“自由发挥”，要做成“有限工种”
一期只做 5 个核心技能，避免过早泛化：
- `requirements_spec_skill`
  输入：项目简介、用户原始需求
  输出：结构化需求规格说明书 Markdown
- `feature_tree_skill`
  输入：需求规格说明书
  输出：功能树、用户故事、验收标准
- `page_structure_skill`
  输入：功能树、应用类型
  输出：页面树、页面目标、路由、模板类型
- `wireframe_skill`
  输入：页面结构、功能映射、应用类型
  输出：草图模块列表和画布元素
- `html_prototype_skill`
  输入：确认后的草图、选定样式方向
  输出：单页或多页 HTML 原型

每个技能都必须只产出一种主结果，不做跨阶段混产。这样 Claude/Codex 换模型时，漂移会小很多。

### 3. 输出必须结构化，不能靠纯 prompt 猜
一期统一要求模型输出 `JSON + 可读摘要`，其中 JSON 为系统真值，可读摘要给用户看。不要让模型直接自由生成长文本后再猜解析。

重要接口与类型调整：
- 新增 `AIWorkflowRun`
  记录当前 run、阶段、输入快照、输出快照、状态、错误
- 新增 `SkillExecution`
  记录技能名、模型、prompt 版本、schema 版本、重试次数
- 扩展现有领域对象，直接复用你现在已有的类型方向：
  - `RequirementDoc`
  - `FeatureTree`
  - `PageStructureNode`
  - `WireframeDocument`
  - `UISpecDoc`
- 新增 `StyleProfile`
  存储用户选择的视觉风格，如行业风格、颜色倾向、端类型、参考品牌

结构化输出约束固定为：
- 文档类：返回 `title / summary / sections`
- 树类：返回 `nodes[]`，每个节点必须有 `name / description / children`
- 页面类：返回 `pages[]`，每页必须有 `name / route / goal / template / featureIds`
- 草图类：返回 `modules[]`，每个模块必须有 `name / content / x / y / width / height`
- HTML 类：返回 `pages[]`，每页必须有 `path / title / html / cssTokensUsed`

### 4. 模型策略采用“协议优先，不绑品牌”
Claude/Codex 可以接，但只作为 provider：
- `Claude` 更适合需求整理、PRD、功能拆解
- `Codex/OpenAI-compatible` 更适合工具调用、工程化输出、后续代码阶段
- 产品层不暴露“Claude 模式”或“Codex 模式”，只暴露：
  - `标准`
  - `高质量文档`
  - `高质量执行`
- 后台再映射到具体模型

一期默认推荐：
- 文档阶段：Claude Sonnet 类模型优先
- 草图和 HTML 阶段：OpenAI-compatible/Codex 风格模型优先
- 若只保留单模型通路，先做 `Claude 或 GPT/4.1/4o 类` 任一稳定 API，避免多模型同时调优

### 5. Prompt 设计采用四层，不要一条大 prompt 走天下
一期 prompt 固定分四层：
- `System Prompt`
  定义角色、禁止项、输出格式、语言风格、不能跳阶段
- `Workflow Prompt`
  说明当前阶段目标、已有输入、可用技能、停止条件
- `Skill Prompt`
  每个技能自己的领域规则、输出 schema、质量标准
- `Reviewer Prompt`
  对技能结果做二次校验，判断是否缺字段、是否有幻觉、是否需要回退重试

Prompt 规则固定：
- 不允许模型自己发明系统里不存在的字段
- 不允许在未确认前跳到下一阶段
- 不允许直接输出最终 UI 代码，除非当前阶段就是 `html_prototype`
- 每次只对当前阶段负责
- 先给结构化结果，再给简短中文解释

建议的一期 prompt 模板：
- `requirements_spec_skill`
  “你是资深产品经理助理。请把用户原始需求整理成可确认的需求规格说明书。不要补充未被暗示的业务规则。缺失项用 assumption 标注，不要伪造确定结论。”
- `feature_tree_skill`
  “你是产品分析师。请把已确认需求拆成功能树，层级不超过 3 层，每个叶子节点必须可被页面或流程承接。”
- `page_structure_skill`
  “你是信息架构设计师。请把功能树映射为页面结构，优先页面目标和用户任务闭环，不要生成实现细节。”
- `wireframe_skill`
  “你是低保真原型设计师。请只输出布局模块，不输出视觉设计词，不输出前端代码。”
- `html_prototype_skill`
  “你是前端原型设计师。请基于已确认草图生成可预览 HTML 原型，保留低到中保真交互，不实现真实业务逻辑。”

### 6. 一期的人机分工必须明确
AI 自动做：
- 需求整理
- 功能拆解
- 页面结构建议
- 草图初稿
- HTML 原型初稿

用户必须确认：
- 需求规格说明书
- 功能清单
- 页面结构
- 草图版本
- 样式方向

用户可编辑的入口固定为：
- 文档编辑器改需求
- 功能树手动增删改
- 页面树手动增删改
- 草图画布手动调整
- 样式选择器选风格后再生成 HTML

### 7. 一期 HTML 导出范围固定，避免失控
一期 HTML 导出只做：
- 静态页面结构
- 假数据占位
- 基础交互态展示
- 多页面跳转关系
- 单独导出 `.html + .css + assets manifest`

一期不做：
- React/Vue 工程代码导出
- 真正业务逻辑
- Figma/Axure 双向还原
- 像素级设计稿同步

Figma/Axure 的位置：
- 第二阶段做“导出适配器”
- 一期先保留中间标准格式，后续再转
- 中间标准格式建议就是 `UISpecDoc + HTML snapshot + StyleProfile`

## Test Plan
必须覆盖这些场景：
- 用户一句模糊需求输入后，系统能生成可确认的需求规格说明书，并标出假设项
- 已确认需求能稳定生成功能树，且每个叶子功能都有验收标准
- 功能树能生成页面结构，页面数不过度膨胀，且页面目标明确
- 页面结构能生成草图模块，模块坐标和尺寸可被现有画布直接消费
- 用户手改草图后，再生成 HTML 时优先使用“确认后的草图”而不是旧需求
- 批处理到阶段边界时会自动停下，不越过确认点
- 模型输出缺字段或格式错误时，reviewer 能拦截并触发重试或人工提示
- 更换 Claude/Codex/OpenAI-compatible provider 后，系统仍能得到同一套结构化结果
- HTML 导出结果可在本地直接预览，并与草图结构基本一致

## Assumptions
- 一期目标用户是产品经理，不是工程师
- 一期最重要的是“降低沟通成本 + 快速形成可确认产物”，不是直接生成生产代码
- 默认走 `统一 Agent 协议`，不封装 Claude/Codex 官方客户端
- 默认主交付是 `需求文档 -> 功能树 -> 页面草图 -> HTML 原型`
- 默认采用 `半自动批处理 + 阶段确认`
- 默认只优先打通 HTML 导出，Figma/Axure 作为后续导出层
- 第二期再进入“设计到代码”，第三期再收敛到可视化 vibecoding
